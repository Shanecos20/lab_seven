import React from 'react';

// Defining the Header component to display a heading
const Header = () => {
    return <h1 className="text-center text-primary">My Header in another component</h1>; // Returning a simple heading
};

export default Header; // Exporting the Header component for use in other components
