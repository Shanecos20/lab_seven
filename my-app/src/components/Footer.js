import React from 'react';

// Defining the Footer component to display a footer message
const Footer = () => {
    return <h3 className="text-center text-primary">My Footer in another component</h3>; // Returning a simple footer message
};

export default Footer; // Exporting the Footer component for use in other components
